import { deepEqual } from 'assert';
import { playground } from '../playground';

describe('playground', () =>
{
  it('should return correct results', () =>
  {
    // Replace the next dummy test with your playground function tests.
    deepEqual(playground(), 120);
  });
});
